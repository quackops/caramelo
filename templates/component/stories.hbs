import type { Meta, StoryObj } from '@storybook/react-vite';

import { fn } from 'storybook/test';

import { {{properCase title}} } from './{{dashCase title}}';

export default {
  title: 'interactive/{{properCase title}}',
  component: {{properCase title}},
  parameters: {
    layout: 'centered',
  },
  args: { onClick: fn() },
} satisfies Meta<typeof {{properCase title}}>;

export const Default = {
  args: {
    children: 'Hello World',
  },
} satisfies StoryObj<typeof {{properCase title}}>;
